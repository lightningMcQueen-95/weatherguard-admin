# In development the Vite dev server proxies /api to the NestJS API
# (see vite.config.ts), so no env var is needed locally.
#
# For a production build served separately from the API, set:
VITE_API_URL=https://your-api-domain.com
